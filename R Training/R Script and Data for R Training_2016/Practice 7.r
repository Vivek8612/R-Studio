# Practice 7

# Add the following to the returnSubset function
# Create a variable lowHighVar...Its values is High if TOS > 5000 else Low...this variable should be added to the outDataFrame
# Use a FOR loop to find the unique values (use unique command to find unique) of each variable in the outDataFrame and add it to a LIST
# Return the list instead of the dataframe - The returned list should contain the unique values in each variable


